import React, {useEffect, SFC, useState} from 'react';

type NeutralProps = {
  data: any;
};

const Neutral: SFC<NeutralProps> = (props) => {

  return (
    <>
      <div>props</div>
    </>
  );
};

export default Neutral;
